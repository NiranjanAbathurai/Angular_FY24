import { TestimpurePipePipe } from './testimpure-pipe.pipe';

describe('TestimpurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TestimpurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
